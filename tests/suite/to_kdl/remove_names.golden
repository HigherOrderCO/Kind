// List.nil #List.nil -(a: Type) : (List _)
ctr {List.nil}

// List.cons #List.cons -(a: Type) (head: a) (tail: (List a)) : (List _)
ctr {List.cons head tail}

// Ora.Ora.ora #Ora.Ora.ora (h: (List U60)) : (List U60)
fun (Ora.Ora.ora h) {
  (Ora.Ora.ora {List.cons ~ xs}) = let a = #2; let b = #3; let c = #4; let ~ = (+ c b); {List.cons a xs}
}

