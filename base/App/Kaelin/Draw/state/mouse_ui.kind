App.Kaelin.Draw.state.mouse_ui(
  info: App.EnvInfo,
  img: VoxBox
): VoxBox
  open info 
  coord = App.Kaelin.Coord.to_axial(info.mouse_pos)
  {x,y} = App.Kaelin.Draw.support.centralize(coord)
  VoxBox.Draw.image(x, y, 0#32, App.Kaelin.Assets.tile.mouse_ui, img)
