App.Kaelin.Draw.creature.ap(
  cx: U32,
  cy: U32, 
  creature: App.Kaelin.Creature,
  img: VoxBox
): VoxBox

  ap = I32.to_int(creature@ap)
  ap = Nat.show(Int.to_nat(ap))
  VoxBox.Draw.text(ap, PixelFont.small_black, Pos32.new(cx,cy,0u), img)
