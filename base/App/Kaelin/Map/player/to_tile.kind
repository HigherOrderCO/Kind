App.Kaelin.Map.player.to_tile(address: String, map:App.Kaelin.Map): Maybe<App.Kaelin.Tile>
  coord = App.Kaelin.Map.player.to_coord(address,map)
  case coord{
    none: none
    some: 
      App.Kaelin.Map.get(coord.value, map)
  }