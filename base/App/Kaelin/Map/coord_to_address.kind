App.Kaelin.Map.coord_to_address(
  player_coord: App.Kaelin.Coord,
  map: App.Kaelin.Map
): Maybe<String>

result = Maybe { 
  get tiles = App.Kaelin.Map.get(player_coord, map)
  get creature = tiles@creature
  return creature@player
  }
result <> none
