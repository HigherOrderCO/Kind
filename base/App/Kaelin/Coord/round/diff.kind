App.Kaelin.Coord.round.diff(
  x: F64,
  y: F64
): F64 // while F64.abs doesnt exist

  big_number = 1000.0 // while F64.gtn, cmp and ltn is broken
  x = x + big_number
  y = y + big_number

  case F64.gtn(x, y) {
    true: x - y
    false: y - x
  }
