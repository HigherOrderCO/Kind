App.Kaelin.Coord.draft.to_map(team: String, coords: Pair<NatMap<String>,NatMap<String>>): NatMap<String>
switch String.eql(team) {
  "blue" : coords@fst
  "red"  : coords@snd
}default NatMap.new<String>
