//Finds a hero in the map within a coord
App.Kaelin.Coord.coord_to_hero(
  coord: App.Kaelin.Coord,
  map: App.Kaelin.Map
): Maybe(App.Kaelin.Hero)

  Maybe {
  get tile = App.Kaelin.Map.get(coord, map)
  get creature = tile@creature
  return creature@hero
  }
      
