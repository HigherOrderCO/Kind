App.Kaelin.Coord.Convert.axial_to_cubic(
  coord: App.Kaelin.Coord
): App.Kaelin.Coord.Cubic

    open coord
    x = coord.i
    z = coord.j
    y = (I32.neg(x) - z)  //I32

    App.Kaelin.Coord.Cubic.new(x, y, z)
