App.Kaelin.App.draw(img: VoxBox): App.Draw<App.Kaelin.State>
(state)
  global = state@global
  stage = global@stage
  case stage {
    init: App.Kaelin.Draw.init
    draft: App.Kaelin.Draw.draft
    planning: DOM.vbox({}, {}, App.Kaelin.Draw.state(img, state))
  } default DOM.text("Em breve")
