

App.Kaelin.App.test : _
  let ls = [0, 1, 2]
  let list = List.foldr!!([], 
    (y) List.concat!(List.foldr!!([], (x) List.cons!({y, x}), ls)), ls)
  let list = List.chunks_of!(3, list)
  let list = List.sequenceA!(list)
  List.show!(List.show!(Pair.show!!(Nat.show, Nat.show)), list)