App.KL.Game.Effect.coord.get_target: App.KL.Game.Effect<Hexagonal.Axial>
  (center, targets, caster_id, map)
  let target = List.head!(targets) <> center

  App.KL.Game.Effect.Result.new!(target, center, caster_id, map, Hexagonal.Axial.Map.new!, 0, [], "", {})
