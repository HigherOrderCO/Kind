App.KL.Game.Board.Creature.show_hp(
  cx: U32,
  cy: U32, 
  creature: App.KL.Game.Creature,
  img: VoxBox
): VoxBox

  hp = I32.to_int(creature@hp)
  hp = Nat.show(Int.to_nat(hp))
  VoxBox.Draw.text(hp, PixelFont.small_black, Pos32.new(cx,cy,0u), img)
