App.KL.Game.Creature.Status.haste.check(
  creature: App.KL.Game.Creature
): Bool

  let status = creature@status
  case status {
    nil:
      false
    cons:
      case status.head { 
        haste: if I32.gtn(status.head.duration, 0) then true   
              else false
      }default false
  }
