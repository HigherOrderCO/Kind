App.Kaelin.Coord.Cubic.add(
  coord_a: App.Kaelin.Coord.Cubic,
  coord_b: App.Kaelin.Coord.Cubic
): App.Kaelin.Coord.Cubic

  open coord_a
  open coord_b
  x = coord_a.x + coord_b.x // I32
  y = coord_a.y + coord_b.y // I32
  z = coord_a.z + coord_b.z // I32
  App.Kaelin.Coord.Cubic.new(x, y, z)
