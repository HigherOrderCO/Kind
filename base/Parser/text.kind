Parser.text(text: String): Parser(Unit)
  (long, idx, code) case Parser.text.go(text, long, idx, code) as reply {
    error:
      Parser.Reply.error<Unit>(reply.val),
    value:
      Parser.Reply.value<Unit>(reply.long, reply.idx, reply.code, reply.val),
  }
