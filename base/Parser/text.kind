Parser.text(text: String): Parser(Unit)
  (err, idx, code) case Parser.text.go(text, err, idx, code) as reply {
    error:
      Parser.Reply.error<Unit>(reply.err),
    value:
      Parser.Reply.value<Unit>(reply.err, reply.idx, reply.code, reply.val),
  }
