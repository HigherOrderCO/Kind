Parser.append_error<V: Type>(parse: Parser(V), suff: String): Parser(V)
  (long, idx, code)
    let reply = parse(long, idx, code)
    case reply {
      error:
        open reply.err
        Parser.Reply.error_long!(reply.err.idx, reply.err.code, reply.err.err | suff)
      value: reply
    }
