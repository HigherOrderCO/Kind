Parser.override_error<V: Type>(parse: Parser(V), error: String): Parser(V)
  (long, idx, code)
    let reply = parse(long, idx, code)
    case reply {
      error:
        open reply.val
        Parser.Reply.error_long!(reply.val.idx, reply.val.code, error)
      value: reply
    }
