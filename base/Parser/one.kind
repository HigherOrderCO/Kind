Parser.one: Parser(Char)
  (long, idx, code) case code {
    nil: Parser.Reply.error_long<Char>(idx, code, "Unexpected end of file."),
    cons: Parser.Reply.value<Char>(long, Nat.succ(idx), code.tail, code.head),
  }
