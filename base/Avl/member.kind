Avl.member<K: Type, V: Type>(cmp: K -> K -> Cmp, key: K, map: Avl<K,V>): Bool
  case map {
    tip: Bool.false,
    bin: case cmp(key, map.key) {
      ltn: Avl.member<K,V>(cmp, key, map.left),
      eql: Bool.true,
      gtn: Avl.member<K,V>(cmp, key, map.right)
    }
  }