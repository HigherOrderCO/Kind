Avl.delete_min<K: Type, V: Type>(map: Avl<K,V>): Avl<K,V>
  case map {
    tip: map,
    bin: case map.left {
      tip: map.right,
      bin:  
        let new_left = Avl.delete_min<K,V>(map.left)
        Avl.balance<K,V>(map.key, map.val, new_left, map.right)
    }
  }