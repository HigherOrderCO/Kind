get_interval(random : F64, min : Nat, max : Nat) : Nat
  F64.to_nat(random * (Nat.to_f64(max) - Nat.to_f64(min)) + Nat.to_f64(min))


User.caotic.test : IO<_>
  IO {
    get a = Crypto.random
    IO.print(Nat.show(get_interval(a, 1, 10)))
  }