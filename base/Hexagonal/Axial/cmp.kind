// Returns the comparison between two coordinates
Hexagonal.Axial.cmp(
  x: Hexagonal.Axial,
  y: Hexagonal.Axial
): Cmp

  let comparison = I32.cmp(x@i, y@i)
  case comparison {
    eql: I32.cmp(x@j, y@j)
  } default comparison
