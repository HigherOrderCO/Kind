// Inserts a value to a Map tree
Hexagonal.Axial.Map.set<A : Type>(
  key : Hexagonal.Axial,
  val : A,
  map : Hexagonal.Axial.Map<A>
): Hexagonal.Axial.Map<A>

  BBT.insert!!(Hexagonal.Axial.cmp, key, val, map)
