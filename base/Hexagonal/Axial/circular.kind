// Returns a list of coordinates within a given range minus the central coord
Hexagonal.Axial.circular(
  coord: Hexagonal.Axial
  distance: I32
): List<Hexagonal.Axial>

  range = Hexagonal.Axial.range(coord, distance)


