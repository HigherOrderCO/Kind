Ether.tests : _
  let encod = Ether.RLP.encode(Ether.RLP.Tree.tip(Nat.to_bits(10)))
  log(encod)
  Ether.RLP.decode(encod)

  //let buffer = Buffer8.from_hex("d69429d7d1dd5b6f9c864d9db560d72a247c178ae86b0a")
  //let hash = Crypto.Keccak.hash.bytes(buffer)
  //Buffer8.to_hex(hash)
