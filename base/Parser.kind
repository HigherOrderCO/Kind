Parser(V: Type): Type
  Maybe(Parser.ErrorAt) -> // error that got the furthest in the code
                           // shouldn't be used inside a parser, only in monads,
                           // this changes unpredictably
  Nat -> String -> Parser.Reply<V>
