// Check which is the current page. 
// TODO: improve it
Web.Kind.helper.is_current(title: String, page: Web.Kind.Page): Bool
  case page{
    home: String.eql(title, "Home")
    apps: String.eql(title, "Apps")
  }
