// function to draw according the state
Web.Kaelin.Draw.state(img: Image3D, state: Web.Kaelin.State): Image3D
  case state {
      game:
        let new_img = Web.Kaelin.Draw.state.players(img, state.players)
        new_img
  } default img