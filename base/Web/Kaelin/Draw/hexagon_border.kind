// Not being used
// draws the hexagon border
Web.Kaelin.Draw.hexagon_border(
  cx: U32,
  cy: U32,
  rad: U32,
  col: Col32,
  img: VoxBox,
) : VoxBox 
//   let draw_e = Bool.or(Bool.and(U32.eql(U32.mod(j,2#32),0#32),U32.eql(i,0#32)),U32.eql(j,4#32))
//   let draw_f = Bool.or(Bool.and(U32.eql(U32.mod(j,2#32),1#32),U32.eql(i,4#32)),U32.eql(j,4#32))
  let img = VoxBox.Draw.deresagon(cx,cy,0#32,rad,col,true,true,true,true,true,true,img)
  img
