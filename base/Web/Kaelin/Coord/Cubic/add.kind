Web.Kaelin.Coord.Cubic.add(a: Web.Kaelin.Coord.Cubic, b: Web.Kaelin.Coord.Cubic): Web.Kaelin.Coord.Cubic
  open a
  open b
  let x = Int.add(a.x, b.x)
  let y = Int.add(a.y, b.y)
  let z = Int.add(a.z, b.z)
  Web.Kaelin.Coord.Cubic.new(x, y, z)