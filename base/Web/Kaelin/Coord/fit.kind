Web.Kaelin.Coord.fit(
  coord: Web.Kaelin.Coord,
  map_size: U32
) : Bool
  open coord
  let i = coord.i
  let j = coord.j

  let sum = I32.add(i, j)
  let abs = I32.abs(sum)
  let abs = I32.to_u32(abs)
  U32.lte(abs, map_size)
