Vector.from_list(A : Type, ls : List<A>) : Vector<A, List.length!(ls)>
  case ls {
      nil : Vector.nil!
      cons : 
        let pred = Vector.from_list(A, ls.tail)
        Vector.cons(A, _, ls.head, pred)
  }!