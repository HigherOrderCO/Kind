Vector.fill(A : Type, v : A, size : Nat) : Vector<A, size>
  case size {
      zero : Vector.nil(A)
      succ : 
        let rec = Vector.fill(A, v, size.pred) :: Vector(A, size.pred)
        Vector.ext(A, size.pred, v, rec) :: Vector(A, Nat.succ(size.pred))
  }!