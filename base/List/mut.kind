List.mut<A: Type>(idx: Nat, func: A -> A, xs: List<A>): List<A>
  case xs {
    nil: List.nil!
    cons: case idx {
      zero: List.cons<A>(func(xs.head), xs.tail)
      succ: List.cons<A>(xs.head, List.mut<A>(idx.pred, func, xs.tail))
    }
  }