List.delete_by<A: Type>(cond: A -> A -> Bool, a: A, xs: List<A>): List<A>
  case xs {
    nil: List.nil!
    cons: case cond(a, xs.head) {
      true: List.delete_by!(cond, a, xs.tail)
      false: List.cons!(xs.head, List.delete_by!(cond, a, xs.tail))
    }
  }