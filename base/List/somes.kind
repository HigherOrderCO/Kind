List.somes<A: Type>(xs: List<Maybe<A>>): List<A>
  case xs {
    nil: List.nil!
    cons: case xs.head {
      none: List.somes<A>(xs.tail)
      some: List.cons!(xs.head.value, List.somes<A>(xs.tail))
    }
  }
