// Deletes the first element that satisfy a condition 
List.delete_el<A: Type>(cond: A -> A -> Bool, a: A, xs: List<A>): List<A>
  case xs {
    nil: List.nil!
    cons: case cond(a, xs.head){
			true: xs.tail
      false: List.cons!(xs.head, List.delete_el!(cond, a, xs.tail))
    }
  }
