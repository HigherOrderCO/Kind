List.zip_with<A: Type, B: Type, C: Type>(f: A -> B -> C, xs: List<A>, ys: List<B>)
  : List<C>
  case xs {
    nil: List.nil!
    cons: case ys {
      nil: List.nil!
      cons: List.cons!(f(xs.head, ys.head),List.zip_with!!!(f, xs.tail, ys.tail))
    }
  }