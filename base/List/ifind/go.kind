List.ifind.go<A: Type>(xs: List<A>, f: Nat -> A -> Bool, idx: Nat): Maybe<Pair<Nat,A>>
  case xs {
    nil: none
    cons: case f(idx, xs.head) {
      true: some({idx, xs.head})
      false: List.ifind.go!(xs.tail, f, Nat.succ(idx))
    }
  }
