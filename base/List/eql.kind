List.eql<A: Type>(eql: A -> A -> Bool, xs: List<A>, ys: List<A>): Bool
  case xs ys {
    nil nil: true
    cons cons: Bool.and(eql(xs.head, ys.head), List.eql<A>(eql, xs.tail, ys.tail))    
  } default false 