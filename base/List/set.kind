List.set<A: Type>(idx: Nat, val: A, xs: List<A>): List<A>
  case xs {
    nil: List.nil!
    cons: case idx {
      zero: List.cons!(val, xs.tail)
      succ: List.cons!(xs.head, List.set<A>(idx.pred, val, xs.tail))
    }
  }
