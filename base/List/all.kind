// Receives a condition, a as, an returns true if 
// all the elements of that as satisfy the condition.
List.all<A: Type>(cond: A -> Bool, xs: List<A>): Bool
  case xs{
    nil: true
    cons: case cond(xs.head){
      true: List.all!(cond, xs.tail)
      false: false
    }
  }
