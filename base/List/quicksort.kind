List.quicksort(xs: List<Nat>): List<Nat>
  case xs {
    nil: List.nil!
    cons:
      let fst = xs.head
      let min = List.filter!((x) x <? xs.head, xs.tail)
      let max = List.filter!((x) x >? xs.head, xs.tail)
      List.quicksort(min) ++ [fst] ++ List.quicksort(max)
  }
