List.delete_at<A: Type>(idx: Nat, xs: List<A>): List<A>
  case idx {
    zero: List.tail<A>(xs)
    succ: case xs{
      nil: xs
      cons: List.cons<A>(xs.head, List.delete_at<A>(idx.pred, xs.tail))
    }
  }
