List.count<A: Type>(cond: A -> Bool, xs: List<A>): Nat
  case xs {
    nil: 0
    cons: case cond(xs.head) {
      true: Nat.succ(List.count<A>(cond, xs.tail))
      false: List.count<A>(cond, xs.tail)
    }  
  }
