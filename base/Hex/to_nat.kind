Hex.to_nat (x : String): Nat
  case (Parser.run!(Hex.to_nat.parser, x)) as k {
    some : k.value
    none : 0
  }

Hex.to_nat.parser: Parser(Nat)
  Parser {
    Parser.maybe!(Parser.text("0x"))
    var digits = Parser.many1<Nat>(Parser.hex_digit)
    return Nat.from_base(16, digits)
  }


