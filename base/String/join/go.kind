String.join.go(sep: String, xs: List<String>, fst: Bool): String
  case xs {
    nil: ""
    cons: String.flatten([
      if fst then "" else sep,
      xs.head, 
      String.join.go(sep, xs.tail, Bool.false),
    ])
  }