String.take_while(f: Char -> Bool, str: String): String
  case str {
    nil : String.nil
    cons: case f(str.head) {
      true : String.cons(str.head, String.take_while(f,str.tail))
      false: String.nil
    }
  }
