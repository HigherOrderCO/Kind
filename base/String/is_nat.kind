String.is_nat(str: String): Bool
  case str {
    nil: false
    cons: case str.tail {
      nil: Char.is_digit(str.head) 
      cons: Bool.and(Char.is_digit(str.head), String.is_nat(str.tail))
    }
  }
  