String.remove_start_with.go(str: String, match: String): String
  case match{
    nil : str
    cons: case str{
      nil : String.nil //str < match
      cons: case Char.eql(match.head, str.head){
        true : String.remove_start_with.go(str.tail, match.tail)
        false: str.tail
      }
    }
  }