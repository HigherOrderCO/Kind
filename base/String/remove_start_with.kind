String.remove_start_with(str: String, match: String): String
  case str{
    nil : String.nil
    cons: case String.starts_with(str, match){
      true : String.remove_start_with.go(str, match)
      false: str
    }
  }