String.eql_no_sensitive(as: String, bs: String): Bool
  case as bs {
    nil nil: true
    cons cons: 
      if U16.eql(Char.to_lower(as.head), Char.to_lower(bs.head)) then
        String.eql_no_sensitive(as.tail, bs.tail)
      else false
  } default false