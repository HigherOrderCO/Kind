// checks if all characters in a string are hexadecimal digits 
String.is_hex(str: String): Bool
  case str {
    nil: false
    cons: case str.tail {
      nil: Char.is_hex(str.head) 
      cons: Bool.and(Char.is_hex(str.head), String.is_hex(str.tail)) 
    } 
  }