String.remove_spaces(str: String): String
  case str {
    nil: String.nil
    cons: 
      if Char.eql(str.head, ' ') then
        String.remove_spaces(str.tail)
      else
        String.cons(str.head, String.remove_spaces(str.tail))
  }