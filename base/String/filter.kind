String.filter(f: Char -> Bool, str: String): String
  case str{
    nil : String.nil
    cons: case f(str.head){
      true : String.cons(str.head,String.filter(f, str.tail))
      false: String.filter(f, str.tail)
      }
  }