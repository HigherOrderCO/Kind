String.starts_with(str: String, match: String): Bool
  case match {
    nil : Bool.true
    cons: case str {
      nil : Bool.false
      cons: case Char.eql(match.head, str.head){
        true : String.starts_with(str.tail, match.tail)
        false: Bool.false
      }
    }
  }