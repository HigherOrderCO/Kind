Map.show<A: Type>(
  show: A -> String
  map: Map<A>
): String
  let ret = "{"
  let value_list = Map.to_list!(map)
  for p in value_list with ret:
    open p
    ret|"\n  \""|p.fst|"\": "|show(p.snd)
  ret|"\n}"
