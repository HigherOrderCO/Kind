Pair.show<A: Type, B: Type>(
  show_a: A -> String,
  show_b: B -> String,
  p: Pair<A,B>
) : String
  case p{ 
    new: 
      let fst_str = show_a(p.fst) 
      let snd_str = show_b(p.snd) 
      let str     = "(" | fst_str | "," | snd_str | ")"
      str   
  }