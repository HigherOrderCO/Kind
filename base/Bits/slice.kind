Bits.slice(len: Nat, bs: Bits): Bits
  case len {
    zero: Bits.e
    succ: case bs {
      e : Bits.e
      o : Bits.o(Bits.slice(len.pred, bs.pred))
      i : Bits.i(Bits.slice(len.pred, bs.pred))
    }
  }