// Multiplies two natural numbers
// - a: The 1st nat.
// - b: The 2nd nat.
// = The product of `a` and `b`
Nat/mul
: ∀(a: Nat)
  ∀(b: Nat)
  Nat
= λ{
  #zero: λb
    #zero{}
  #succ: λa.pred λb
    (Nat/add b (Nat/mul a.pred b))
}
