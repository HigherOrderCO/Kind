// The identity function for natural numbers.
// - n: The input natural number.
// = The same natural number `n`.
Nat/id
: ∀(n: Nat)
  Nat
= λ{
  #zero:
    #zero{}
  #succ: λn.pred
    #succ{(Nat/id n.pred)}
}
