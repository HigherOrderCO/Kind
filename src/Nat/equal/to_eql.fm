Nat.equal.to_eql(a: Nat, b: Nat, e: a == b): Nat.eql(a, b) == true
  let p0 =
    case a {
      zero: refl
      succ: Nat.equal.to_eql(a.pred, a.pred, refl)
    }! 

  p0 :: rewrite x in Nat.eql(a, x) == true with e
